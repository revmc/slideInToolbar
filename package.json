{
  "name": "slide-in-toolbar",
  "version": "1.0.0",
  "description": "just messing around with slide-in toolbars",
  "main": "scripts.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "jshint **.js",
    "serve": "browser-sync start --server --files '*.html, *.js, *.css'",
    "build:sass": "node-sass -r sass -o .",
    "watch:sass": "npm run build:sass && npm run build:sass -- -w"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/revmc/slideInToolbar.git"
  },
  "keywords": [
    "toolbar"
  ],
  "author": "shaque",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/revmc/slideInToolbar/issues"
  },
  "homepage": "https://github.com/revmc/slideInToolbar#readme",
  "devDependencies": {
    "browser-sync": "^2.17.5",
    "jshint": "^2.9.4",
    "node-sass": "^3.10.1"
  }
}
